# Status：安卓 markdown 表格消息截断且无「查看更多」

> 最后更新：2026-08-20 ｜ 图例：⬜ 未开始 · 🚧 进行中 · ✅ 完成 · ❌ 阻塞

## 平台矩阵

行号对应 `plan.md` 的 Task。本期**只做安卓**，其余三端不涉及。

| 任务 | web | android | ios | desktop |
|------|-----|---------|-----|---------|
| T1 切分支 + 临时打点（表格是否自身被限高） | — | ⬜ | — | — |
| T2 段栈判定源改 `rawContentHeight`，删按段取舍 | — | ⬜ | — | — |
| T3 provider 四入口统一 cap 语义 | — | ⬜ | — | — |
| T4 静态自查（复用与不变量） | — | ⬜ | — | — |
| T5 构建 onTest 包 + 装机 | — | ⬜ | — | — |
| T6 真机验收 + 采集打点 | — | ⬜ | — | — |
| T7 清临时日志 + 文档收尾 | — | ⬜ | — | — |

## 缺陷速查

| 编号 | 现象 | 路径 | 样本 |
|------|------|------|------|
| A | 表格被像素级硬切、无「查看更多」、高度锁死 480dp | 段栈（含表格） | 群 AI 框「报餐统计」卡片，2026-08-20 10:49 |

根因：`onMeasure` 夹 `getMeasuredHeight()`（含段栈 24dp padding），`isFoldNeeded` 算 Σ子段高（不含），
差 24dp；总高落在 456~480dp 时外框被夹但判定说不用折叠，且该分支漏了 `setHeightCap(0)`。详见 `spec.md`。

## 待办 / 阻塞

- (android) T5/T6 需真机；设备不可用则卡在 T5 Step 2。
- (android) 本仓库无单测，验收全靠真机人工过 9 项矩阵。
- (android) 待验证：截图里横滚指示条可见，怀疑表格 `HorizontalScrollView` 自身也被限高。若成立是**第二个独立缺陷**，本期不修，T6 采数后另开。

## 关键决策记录

- 2026-08-20 折叠策略选「只裁不取舍，对齐 PC」，接受折叠态表格切在行中间；否决「补 padding 保留按段取舍」（会让表格整块 GONE）。
- 2026-08-20 一次性 `post` 折叠判定改为高度变化监听，覆盖异步图片/表格测量导致的高度变化。
- 2026-08-20 范围含引用悬浮、引用预览、合并转发详情三个附加入口，同根因不许漏修。
- 2026-08-20 不许动：`maxHeightDP = 480`、段栈子 View 禁获焦、`setHeightCap` 当帧硬夹、禁长按（后三条是上一轮修「翻历史被拽回」与「长按被吞」的成果）。
