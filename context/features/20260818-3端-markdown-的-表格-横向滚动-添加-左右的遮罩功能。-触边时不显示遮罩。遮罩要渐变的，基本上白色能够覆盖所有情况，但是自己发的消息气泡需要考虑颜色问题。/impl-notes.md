# Impl Notes：三端 markdown 表格横滚左右渐变遮罩

> 平台无关。三端代码已按同一套显隐公式落地，运行时自测（T4 / T7 / T10）尚未做。

## 状态流转

每张表独立计算，不共享状态。

```
无溢出（内容宽 − 可见宽 ≤ 1）
  → 左右罩都不画

有溢出
  → 偏移 ≤ 1          → 只画右罩
  → 距右缘 ≤ 1        → 只画左罩
  → 其余              → 左右都画
```

滚动过程中实时重算，不是松手才变。容器宽度变了（窗口、旋转、折叠）也重算。

## 接口调用时序

无后端接口。

## 边界情况

| 场景 | 预期 |
|------|------|
| 窄表 | 完全无罩 |
| 一条消息多张表 | 各听各的滚动 |
| 贴左 / 中间 / 贴右 | 只右 / 双侧 / 只左 |
| 自己发的淡蓝气泡 | 罩的实色是气泡蓝，不是白 |
| 外链/微信绿气泡 | 罩跟绿，不是白 |
| 列表复用换了一条消息 | 必须重绑颜色，不能把上一张的蓝带到白气泡 |
| 流式打字机 | 期间表格是纯文本，没有横滚容器，也就没有左右罩；流结束成表后再出现 |
| 和「查看更多」上下罩重叠 | 各管各的；横滚罩不挡底部按钮 |
| 详情 / 合并转发 / 回复聚合 | 与会话同一套管线，行为一致 |

## 错误处理策略

遮罩是纯装饰。算错或画失败时最差就是没有提示条，不影响滚动、长按、发送。不要因为遮罩逻辑抛错让整条消息渲染失败。

## 联调坑

- **写死白色渐变叠淡蓝气泡会糊一块白。** 上一轮 iOS「查看更多」已经踩过。左右罩必须跟**当前这条气泡的真实底色**，三端底色 hex 并不完全相同，不要为了对齐绝对值而抄另一端的色值。
- **罩色要跟最终画出来的气泡底，不要跟气泡组件的默认色。** 会话主题可能把默认淡蓝盖成另一套蓝/绿。只把罩色写在默认值上，自己发的宽表右侧会糊一层偏白渐变。在真正改气泡底的那一层同步改罩色。
- **遮罩不能进滚动内容层。** 进了就会跟着单元格一起跑，或贴左时右罩其实在表尾外面、初始视口里看不见。画在滚动容器的**外侧兄弟**上（绝对定位贴视口），不要用 overflow 内部的伪元素。
- **不要另叠可点的遮罩控件。** 会吃横滚和长按。要么 `pointer-events: none`，要么只在绘制阶段画、不进命中树。
- **安卓列表复用：** 每次绑定含表消息都要重设罩色，字段留在控件上会串色。
- **渐变透明端必须同色、仅 alpha=0。** 走到「透明黑」时，插值中间帧 RGB 往 0 走，白/淡蓝/外链绿气泡上会罩一层脏灰。透明端要带着气泡实色的 RGB，只把不透明度降到 0。PC 用实色条再 mask 成渐变，避免 `transparent` 关键字插值发灰。

## 与 bridge 的交互

无。
