---
description: 禁止擅自 push 到联调/发布分支
alwaysApply: true
---

# Git Push 分支约定

本工作区（含 `apps/web`、`apps/android`、`apps/ios`、`apps/desktop` 及 context）**禁止擅自 push 到联调/发布分支**。

## 必须遵守

- **禁止** push 到 `test-202512`、`release`、`main`/`master` 或其他联调/发布分支（即使用户口头说「push 吧」且当前 checkout 在那些分支上）。
- 若用户要推上述联调/发布分支：先 **明确拒绝并提醒本约定**，等用户再次明确指定分支名后再推。
- 合入 test / release 由人在远端 MR/本地自行处理；Agent **不要**主动在那些分支上 commit + push。

## 反例（已发生过，禁止再犯）

- 在 `test-202512` 修完丢树后直接 `git push origin test-202512` —— **违规**。应 push 当前功能分支，合入 test 由人处理。

## PC 端（apps/desktop）提交禁忌

提交 desktop 时 **禁止** stage / commit 以下本地调试文件（已误提交过，勿再犯）：

- `.env.test`
- `electron-builder.yml`
- `package.json`
- `package-lock.json`

只提交业务源码与单测；上述文件有本地改动时先还原或确保未 stage。
