---
description: Android 真机调试默认装测试环境 onTest，勿默认 develop
globs: apps/android/**
alwaysApply: false
---

# Android 真机调试

用户说「真机调试 / 装包 / 跑真机 / zhixin-run-android」时：

1. **直接执行** `bash .cursor/commands/scripts/zhixin-run-android.sh`（Cursor 指令 `/zhixin-run-android`），不要读 skill、不要自行拼命令。
2. 默认 flavor = `onTest`；仅用户明确说 develop 时加 `--develop`。
3. 完整约定见 `context/platforms/android.md`「真机调试流程」。
